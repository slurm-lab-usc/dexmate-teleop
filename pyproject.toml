[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "omniteleop"
version = "0.5.1"
description = "Universal teleoperation system for robot control"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "AGPL-3.0"}
authors = [
    {name = "Dexmate Team", email = "contact@dexmate.ai"},
]
keywords = ["robotics", "teleoperation", "exoskeleton", "robot-control"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: GNU Affero General Public License v3",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering",
]

dependencies = [
    "numpy>=1.20.0",
    "scipy>=1.7.0",
    "loguru>=0.6.0",
    "rich>=13.0.0",
    "tyro>=0.5.0",
    "pyyaml>=6.0",
    "dexcontrol>=0.5.0,<0.6",  # Requires local fork branch with validated arm-mode responses + read-only construction
    "pytransform3d>=3.14.2",
    "python-socketio>=5.13.0",
    "rerun-sdk>=0.15.0",  # Rerun visualization for robotics data
    "opencv-python>=4.5.0",  # For image processing in recorder
    "dexcomm>=0.4.0",
    # Motion planning stack for the follower. Linux-only markers: ompl ships no
    # macOS wheels, and a Mac is only ever a leader machine (exoskeleton +
    # Joy-Cons + GUI) — the follower runs on the robot. To do planning work on
    # macOS anyway, install these by hand (e.g. `brew install ompl`).
    "dexmotion>=0.4.0; sys_platform == 'linux'",
    "ompl==1.7.0; sys_platform == 'linux'",
    "hatch",
    "joycon-lib",  # brings in evdev on Linux / hidapi elsewhere
    "fastapi>=0.100.0",
    "dextop>=0.5.0,<0.6",  # topic list CLI compatible with 0.5.x
    "uvicorn[standard]>=0.20.0",
    "pynput>=1.7.0",
]

[project.optional-dependencies]

dev = [
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
    "ruff",
    "isort>=5.12.0",
    "pyright",
    "pre-commit",
]

data = [
    "dexdata>=0.1.0,<=0.2.0",
]

vr = [
    "dexstream",
]

[project.scripts]
omni-arm = "omniteleop.leader.arm_reader:main"
omni-joycon = "omniteleop.leader.joycon_reader:main"
omni-paddle = "omniteleop.leader.paddle_leader:main"
omni-cmd = "omniteleop.follower.command_processor:main"
omni-robot = "omniteleop.follower.robot_controller:main"
omni-telemetry = "omniteleop.tools.telemetry_viewer:main"
omni-recorder = "omniteleop.record.mdp_recorder:main"
omni-mcap-recorder = "omniteleop.record.mcap_recorder:main"
omni-wrist-cameras = "omniteleop.record.wrist_camera_adapter:main"


[tool.hatch.build.targets.wheel]
packages = ["src/omniteleop", "internal"]

[tool.hatch.build.targets.sdist]
include = [
    "src/omniteleop",
    "README.md",
    "LICENSE",
]

[tool.black]
line-length = 100
target-version = ['py310', 'py311', 'py312', 'py313']

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
